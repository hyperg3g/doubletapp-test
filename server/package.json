{
  "name": "server",
  "version": "1.0.0",
  "scripts": {
    "server": "nodemon index.js",
    "server-prod": "cd ../client && yarn build && cd ../server && nodemon index.js"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.16.4",
    "joi": "^14.3.1",
    "mongodb": "^3.5.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.2"
  }
}
